# Normalize line endings: store as LF in the repo, check out native on each OS.
* text=auto eol=lf

# Windows batch/PowerShell should stay CRLF if any are ever added.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
