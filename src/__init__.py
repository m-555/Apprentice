"""Apprentice — a local, multi-provider code-delegation pipeline (MCP server).

Installed as the `apprentice` package (the repo's `src/` directory). The modules also
run flat from a checkout (`python src/server.py`) — every internal import has a
package/flat fallback shim.
"""

__version__ = "0.2.0"
